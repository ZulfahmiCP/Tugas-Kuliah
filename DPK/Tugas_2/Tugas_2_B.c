#include <stdio.h>

// Nama : Zulfahmi
// NIM : D121231011

int main() {

    int x,y,w;

    x = (2 + 3) - 10 * 2;
    y = (2 + 3) - (10 * 2);
    w = 10 % 3 * 2 + 1;

    printf("Nilai x,y,z : %d, %d, %d\n", x, y, w);

    return 0;
}