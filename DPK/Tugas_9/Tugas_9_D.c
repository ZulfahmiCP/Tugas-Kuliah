#include <stdio.h>
#include <string.h>

int main(){

    char kalimat[100];
    gets(kalimat);

    int panjang = strlen(kalimat);

    while(1){
        if(kalimat[i] == ' ' || kalimat[i] == '\0')
            printf("")
    }

    return 0;
}