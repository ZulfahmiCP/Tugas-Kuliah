#include <stdio.h>
// Nama : Zulfahmi
// NIM : D121231011

int main() {

    int sum,total;
    scanf("%d%d", &sum, &total);

    if(sum == 10 && total < 20)
        printf("Tidak sesuai!\n");

    return 0;
}